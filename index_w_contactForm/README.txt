READ ME

The index.hmtl file contained in this folder includes the modal (and it's trigger) to support a PHP contact form.

The app is being hosted on Firebase (https://chinesenotebook.firebaseapp.com), which requires additional scripts to enable the PHP content.

The contact form will be deployed to the Firebase site after the additional scripts have been added to the project.